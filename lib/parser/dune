(library
  (name arm_parser)
  (modules arm_parser)
  (libraries arm str))

(library
  (name arm_stringifier)
  (modules arm_stringifier)
  (libraries arm))

(library
  (name arm_validator)
  (modules arm_validator)
  (libraries arm_stringifier arm str))

